# ✅ デプロイ前チェックリスト

## 🔍 デプロイ前に確認すること

### **1. 環境変数の確認**

`.env.local` ファイルに以下の環境変数が設定されていることを確認：

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://avaojbznmyigsnorfjoq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=（あなたのキー）

# Stripe Configuration
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51SnaVeK3Uvnn3rbjEjdwFkuXZKie96cMdq7wwake9Dr7c0uwpVFhw6R4ldh63tB2Q8qVKorql7mLPlTlF3jS0lFa00EU3PyDz5
STRIPE_SECRET_KEY=（あなたのキー）

# Resend Configuration
RESEND_API_KEY=re_JJquiei8_4dbgwNYoDpKqRReNpf9LFnpT
```

---

### **2. .gitignore の確認**

`.gitignore` に以下が含まれていることを確認：

- ✅ `.env*` - 環境変数ファイルを除外
- ✅ `/node_modules` - 依存関係を除外
- ✅ `/.next/` - ビルドファイルを除外

---

### **3. ローカルでビルドテスト**

デプロイ前に、ローカルでビルドが成功することを確認：

```cmd
cd C:\projects\ec-shop
npm run build
```

**確認ポイント**:
- ✅ エラーが表示されない
- ✅ ビルドが完了する
- ✅ `.next` フォルダが作成される

---

### **4. 機密情報の確認**

以下の情報がコード内にハードコーディングされていないことを確認：

- ❌ APIキー
- ❌ データベースのパスワード
- ❌ 秘密鍵

**確認方法**:
すべて環境変数（`process.env.XXX`）として参照されていることを確認

---

### **5. 画像ファイルの確認**

`public` フォルダに必要な画像がすべて配置されていることを確認：

- ✅ `logo.png` - ロゴ画像
- ✅ `made-in-heaven-full-pic.jpg` - ヒーロー画像

---

### **6. Supabase画像の確認**

Supabase Storageに商品画像がアップロードされていることを確認：

- ✅ 商品画像（7種類）
- ✅ バケットが「Public」に設定されている

---

### **7. Stripeの設定確認**

Stripeの設定が正しいことを確認：

- ✅ テストモードのAPIキーを使用
- ✅ 本番環境では本番モードのAPIキーに変更する必要がある

---

## 🚀 デプロイ手順

### **ステップ1: GitHubリポジトリの作成**

1. https://github.com にアクセス
2. 新しいリポジトリを作成
3. リポジトリ名: `ec-shop`
4. 「Private」を選択

### **ステップ2: コードをプッシュ**

コマンドプロンプト（cmd）で実行：

```cmd
cd C:\projects\ec-shop

# 初期化（既に初期化されている場合はスキップ）
git init

# 全ファイルをステージング
git add .

# コミット
git commit -m "Initial commit for deployment"

# リモートリポジトリを追加
git remote add origin https://github.com/あなたのユーザー名/ec-shop.git

# メインブランチに変更
git branch -M main

# プッシュ
git push -u origin main
```

### **ステップ3: Vercelでデプロイ**

1. https://vercel.com にアクセス
2. GitHubアカウントでサインアップ/ログイン
3. 「Import Project」をクリック
4. `ec-shop` リポジトリを選択
5. **環境変数を設定**（重要！）
6. 「Deploy」をクリック

---

## ⚠️ 注意事項

### **本番環境で変更が必要な設定**

#### **1. Stripe APIキー**
- テストモード → 本番モードに変更
- `STRIPE_SECRET_KEY` と `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` を本番用に更新

#### **2. Resendの送信元アドレス**
- `onboarding@resend.dev` → `noreply@kirakiraskyfish.com` に変更
- 独自ドメインの検証が必要

#### **3. Supabase RLS（Row Level Security）**
- 本番環境では、適切なRLSポリシーを設定
- 現在は全ユーザーがアクセス可能な状態

---

## 📝 デプロイ後の確認

### **動作確認**

デプロイ後、以下を確認してください：

- [ ] トップページが表示される
- [ ] 商品一覧が表示される
- [ ] 商品画像が表示される
- [ ] 商品詳細ページが表示される
- [ ] カートに商品を追加できる
- [ ] カートページが表示される
- [ ] ログイン/サインアップができる
- [ ] お問い合わせフォームが表示される
- [ ] カタログページが表示される
- [ ] 静的ページ（特商法など）が表示される

### **決済機能のテスト**

Stripeのテストカード番号を使用してテスト：

```
カード番号: 4242 4242 4242 4242
有効期限: 任意の未来の日付
CVC: 任意の3桁
```

---

## 🎉 完了！

デプロイが成功すると、以下のURLでサイトが公開されます：
- https://プロジェクト名.vercel.app

---

## 📞 サポート

デプロイ中に問題が発生した場合：

1. Vercelのビルドログを確認
2. エラーメッセージをコピー
3. エラー内容を調査

よくあるエラー：
- 環境変数が設定されていない
- ビルドエラー（TypeScriptエラーなど）
- 依存関係のインストールエラー

---

準備ができたら、デプロイを開始してください！ 🚀
